Thank you for installing the {{ .Release.Name }} release!

To access the application locally, run the following port-forward command:

  kubectl port-forward svc/nginx-service {{ .Values.port }}:80 -n {{ .Values.namespace }}

Once the port-forwarding is active, open:

  http://localhost:8080

