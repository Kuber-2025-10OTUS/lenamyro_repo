apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: otus-pvc
  namespace: homework
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: otus-storage